import { UseBilling, Context, FactoryParams, CustomQuery } from '../types';
export interface UseBillingParams<BILLING, BILLING_PARAMS> extends FactoryParams {
    load: (context: Context, params: {
        customQuery?: CustomQuery;
    }) => Promise<BILLING>;
    save: (context: Context, params: {
        params: BILLING_PARAMS;
        billingDetails: BILLING;
        customQuery?: CustomQuery;
    }) => Promise<BILLING>;
}
export declare const useBillingFactory: <BILLING, BILLING_PARAMS>(factoryParams: UseBillingParams<BILLING, BILLING_PARAMS>) => () => UseBilling<BILLING, BILLING_PARAMS>;
