import { CustomQuery, Context, FactoryParams, UseMakeOrder } from '../types';
export interface UseMakeOrderFactoryParams<ORDER> extends FactoryParams {
    make: (context: Context, params: {
        customQuery?: CustomQuery;
    }) => Promise<ORDER>;
}
export declare const useMakeOrderFactory: <ORDER>(factoryParams: UseMakeOrderFactoryParams<ORDER>) => () => UseMakeOrder<ORDER>;
