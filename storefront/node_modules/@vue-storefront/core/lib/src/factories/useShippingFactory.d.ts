import { UseShipping, Context, FactoryParams, CustomQuery } from '../types';
export interface UseShippingParams<SHIPPING, SHIPPING_PARAMS> extends FactoryParams {
    load: (context: Context, params: {
        customQuery?: CustomQuery;
    }) => Promise<SHIPPING>;
    save: (context: Context, params: {
        params: SHIPPING_PARAMS;
        shippingDetails: SHIPPING;
        customQuery?: CustomQuery;
    }) => Promise<SHIPPING>;
}
export declare const useShippingFactory: <SHIPPING, SHIPPING_PARAMS>(factoryParams: UseShippingParams<SHIPPING, SHIPPING_PARAMS>) => () => UseShipping<SHIPPING, SHIPPING_PARAMS>;
