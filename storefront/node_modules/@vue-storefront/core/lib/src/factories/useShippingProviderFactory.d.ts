import { UseShippingProvider, Context, FactoryParams, CustomQuery } from '../types';
import { Ref } from '@vue/composition-api';
export interface UseShippingProviderParams<STATE, SHIPPING_METHOD> extends FactoryParams {
    load: (context: Context, params: {
        state: Ref<STATE>;
        customQuery?: CustomQuery;
    }) => Promise<STATE>;
    save: (context: Context, params: {
        state: Ref<STATE>;
        shippingMethod: SHIPPING_METHOD;
        customQuery?: CustomQuery;
    }) => Promise<STATE>;
}
export declare const useShippingProviderFactory: <STATE, SHIPPING_METHOD>(factoryParams: UseShippingProviderParams<STATE, SHIPPING_METHOD>) => () => UseShippingProvider<STATE, SHIPPING_METHOD>;
