'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

var express = require('express');
var express__default = _interopDefault(express);

var app = express__default();
app.use(express.json());
var extend = function (fn) { return fn(app); };
var createMiddleware = function (_a) {
    var apiMiddleware = _a.apiMiddleware;
    if (apiMiddleware && apiMiddleware.extend) {
        extend(apiMiddleware.extend);
    }
    return {
        middleware: {
            path: '/api',
            handler: app
        },
        extend: extend
    };
};

exports.createMiddleware = createMiddleware;
//# sourceMappingURL=index.js.map
