import { CustomQuery, UseCategory, Context, FactoryParams } from '../types';
export interface UseCategoryFactoryParams<CATEGORY, CATEGORY_SEARCH_PARAMS> extends FactoryParams {
    categorySearch: (context: Context, params: CATEGORY_SEARCH_PARAMS & {
        customQuery?: CustomQuery;
    }) => Promise<CATEGORY[]>;
}
export declare function useCategoryFactory<CATEGORY, CATEGORY_SEARCH_PARAMS>(factoryParams: UseCategoryFactoryParams<CATEGORY, CATEGORY_SEARCH_PARAMS>): (id: string) => UseCategory<CATEGORY, CATEGORY_SEARCH_PARAMS>;
