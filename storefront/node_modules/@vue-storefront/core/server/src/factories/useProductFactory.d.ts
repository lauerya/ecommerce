import { CustomQuery, ProductsSearchParams, UseProduct, Context, FactoryParams } from '../types';
export interface UseProductFactoryParams<PRODUCTS, PRODUCT_SEARCH_PARAMS extends ProductsSearchParams> extends FactoryParams {
    productsSearch: (context: Context, params: PRODUCT_SEARCH_PARAMS & {
        customQuery?: CustomQuery;
    }) => Promise<PRODUCTS>;
}
export declare function useProductFactory<PRODUCTS, PRODUCT_SEARCH_PARAMS>(factoryParams: UseProductFactoryParams<PRODUCTS, PRODUCT_SEARCH_PARAMS>): (id: string) => UseProduct<PRODUCTS, PRODUCT_SEARCH_PARAMS>;
