import { CustomQuery, UseUserOrders, Context, FactoryParams } from '../types';
export interface UseUserOrdersFactoryParams<ORDERS, ORDER_SEARCH_PARAMS> extends FactoryParams {
    searchOrders: (context: Context, params: ORDER_SEARCH_PARAMS & {
        customQuery?: CustomQuery;
    }) => Promise<ORDERS>;
}
export declare function useUserOrdersFactory<ORDERS, ORDER_SEARCH_PARAMS>(factoryParams: UseUserOrdersFactoryParams<ORDERS, ORDER_SEARCH_PARAMS>): () => UseUserOrders<ORDERS, ORDER_SEARCH_PARAMS>;
