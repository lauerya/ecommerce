declare const createMiddleware: ({ apiMiddleware }: {
    apiMiddleware: any;
}) => {
    middleware: {
        path: string;
        handler: import("express-serve-static-core").Express;
    };
    extend: (fn: any) => any;
};
export { createMiddleware };
